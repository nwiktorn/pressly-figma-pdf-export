# CLAUDE.md

The project documentation lives in **[AGENTS.md](./AGENTS.md)** (vendor-neutral so
every agent uses the same source). Read it first.

This file exists only so Claude Code auto-loads the pointer.
